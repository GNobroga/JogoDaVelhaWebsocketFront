export enum GameActionType {
  CONNECT = 'connect',
  SEND_INVITE = 'sendInvite',
  RECEIVE_INVITE = 'receiveInvite',
  RECEIVE_ERROR_MESSAGE = 'receiveErrorMessage',
  RECEIVE_MESSAGE = 'receiveMessage',
  RECONNECT = 'reconnect',

}
